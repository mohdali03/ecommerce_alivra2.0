/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://www.alvirabags.in', // Your website URL
    generateRobotsTxt: true, // Optional: Generate a robots.txt file
    sitemapSize: 5000, // Limit entries per sitemap (default is 5000)
  }